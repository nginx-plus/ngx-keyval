language: node_js

node_js:
  - "13"

# Use container-based Travis infrastructure.
sudo: required

addons:
  hosts:
    - your-keyvalue-store.local

branches:
  only:
    - master

install:
  - sudo add-apt-repository -y ppa:nginx/stable
  - sudo apt-get update
  - sudo apt-get install -y --force-yes nginx-extras
  - sudo ls -l /etc/nginx/sites-available/
  - sudo rm /etc/nginx/sites-available/default
  - sudo cp server.conf /etc/nginx/sites-available/
  - sudo ls -l /etc/nginx/sites-available/
  - sudo sed -i 's/\/path\/to\/your\/keyvalue-store\/data\//\/etc\/nginx\/kv\//g' /etc/nginx/sites-available/server.conf
  - sudo ls -l /etc/nginx/sites-available/
  - sudo sed -i 's/deny all;//g' /etc/nginx/sites-available/server.conf
  - sudo ls -l /etc/nginx/sites-available/
  - sudo sed -i 's/listen 80;/listen 127.0.0.1:8080 default_server;/g' /etc/nginx/sites-available/server.conf
  - sudo ls -l /etc/nginx/sites-available/
  - sudo sed -i 's/server_name your-keyvalue-store.local;/ /g' /etc/nginx/sites-available/server.conf
  - sudo ls -l /etc/nginx/sites-available/
  - sudo cat /etc/nginx/sites-available/server.conf
  - sudo ln -s /etc/nginx/sites-available/server.conf /etc/nginx/sites-enabled/server.conf
  - sudo nginx -c /etc/nginx/nginx.conf
  - sudo nginx -t
  #- sudo service nginx start || true
  #- sudo systemctl --full status nginx.service || true

before_script:
  - npm install

script:
  - node server.js &
  - npm test